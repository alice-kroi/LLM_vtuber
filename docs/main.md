创建一个主函数，位置在main.py

主函数功能。

一，解析参数或者命令行参数，

额外参数有开启或者不开启live2d功能，开启或者不开启tts功能，指定的哔哩哔哩直播间号，默认3者都不开启

二，启动哔哩哔哩直播监听程序。我已经写完该程序，功能是将指定直播间的弹幕以openai的**messages，array格式发送往指定端口**

三，监听对应端口，并接受对应的请求

四。持续化langgraph的图，该图的流程如下，1，将请求和对应开启功能的参数输入到状态，2，将openai的**messages发给rag检索信息，3，将rag检索的信息和messages一同进入llm节点，获取回复，4，根据开启功能的参数决定是否再继续发送到其他功能去。**
